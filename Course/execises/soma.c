#include <stdio.h>

int main (){

    int valor, valor2, soma;
  
    printf("Digite o valor de X:");
    scanf("%d", &valor);
    printf("Digite o valor de Y:");
    scanf("%d", &valor2);
    soma = valor + valor2;
    printf("Soma = %d",soma);
  
}